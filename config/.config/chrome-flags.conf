# El wrapper de google-chrome (AUR) lee este archivo. Igual que chromium-flags.conf:
--ozone-platform-hint=auto
# Contrasenas en gnome-keyring (sin esto, almacen "basic" sin cifrar fuera de GNOME/KDE).
--password-store=gnome-libsecret
