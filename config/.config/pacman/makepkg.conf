# Override de usuario de /etc/makepkg.conf (lo leen makepkg y yay).
# Igual que el del sistema pero sin "debug": evita paquetes *-debug de AUR.
OPTIONS=(strip docs !libtool !staticlibs emptydirs zipman purge !debug lto)
